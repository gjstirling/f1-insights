package repositories

import base.ControllersSpecBase

class EventsRepositorySpec extends ControllersSpecBase {

  "[EventsRepository][findAll]" should {

    "should return a list of events" in {
      // Creates a bulkwrites via params
      // calls super method on its abstract class to search the db
    }

  }

  "[EventsRepository][insert]" should {

    "should insert data" in {
      // calls super method on its abstract class to insert into the db
    }

  }
}

